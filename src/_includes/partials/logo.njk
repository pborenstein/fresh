<style>
#logo {
  width: 100%;
  clear: both;
  padding: 0 0em 0.1em 0em;
  margin-bottom: 0rem;
  color: var(--color-heading);
  font-family: var(--font-heading);
    font-size: 4rem;
    line-height: 5rem;
    font-weight: 700;
}

span.slug {
  color: var(--color-red);
}

#logo a[href] {
  text-decoration: none;
  color: inherit;
}

#logo a[href]:hover {
  text-decoration: none;
  color: var(--color-red);
}

#logo a[href]:hover .slug{
  text-decoration: none;
  color: var(--color-heading);
}
</style>

<div id="logo">
  <span>{{config.name}}</span>
</div>
<!-- 
  <a href="/"><span class="slug">example</span>.com</a>
 -->
